# invert (deprecated) (invert)
group: color | deprecated | typical position: legacy stacks only
synonyms: invert, negative, film negative, deprecated, legacy, reverse tones, scan
purpose: A deprecated module that inverted the image relative to a sampled film base
  colour, used for converting scanned negatives.
use_when: only for legacy history stacks; negadoctor replaces it with a proper density
  and print model.
