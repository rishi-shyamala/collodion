# fill light (deprecated) (relight)
group: tone | deprecated | typical position: legacy stacks only
synonyms: fill light, lift shadows, relight, deprecated, legacy, brighten subject, dodge
purpose: A deprecated module that brightened a selected band of the tonal range,
  simulating fill flash.
use_when: only for legacy history stacks; tone equalizer does the same job with an
  edge-aware mask and no halos.
