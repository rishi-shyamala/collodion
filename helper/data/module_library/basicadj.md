# basic adjustments (deprecated) (basicadj)
group: tone | deprecated | typical position: legacy stacks only
synonyms: basic adjustments, exposure contrast saturation, all in one, deprecated, legacy, quick edit
purpose: A deprecated all-in-one module combining exposure, black level, highlight
  compression, contrast, mid-tone brightness and saturation in a single panel.
use_when: only when rendering an old history stack; replace with exposure, filmic rgb
  and color balance rgb for new work.
