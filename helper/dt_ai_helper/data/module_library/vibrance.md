# vibrance (deprecated) (vibrance)
group: color | deprecated | typical position: legacy stacks only
synonyms: vibrance, saturation, deprecated, legacy, boost muted colors, skin safe saturation
purpose: A deprecated module that increased saturation with a weighting that spared
  already-saturated pixels.
use_when: only for legacy history stacks; color balance rgb's global vibrance slider
  replaces it.
