# tone mapping (deprecated) (tonemap)
group: tone | deprecated | typical position: legacy stacks only
synonyms: tone mapping, durand, hdr, local tone mapping, deprecated, legacy, dynamic range
purpose: A deprecated local tone-mapping module based on the Durand bilateral operator.
use_when: only for legacy history stacks; tone equalizer and filmic rgb replace it.
