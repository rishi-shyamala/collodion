# color transfer (legacy) (colortransfer)
group: color | deprecated | typical position: legacy stacks only
synonyms: color transfer, match colors, palette, deprecated, legacy, cluster, copy colors
purpose: A legacy module that transferred colour statistics from a source image to the
  target; replaced by color mapping.
use_when: only for very old history stacks; use color mapping instead.
