# equalizer (legacy) (equalizer)
group: sharpness | deprecated | typical position: legacy stacks only
synonyms: equalizer, wavelet, legacy, deprecated, contrast equalizer old, detail scales
purpose: The original wavelet equaliser, replaced by the edge-aware contrast equalizer
  (atrous).
use_when: only for very old history stacks; use contrast equalizer instead.
