# zone system (deprecated) (zonesystem)
group: tone | deprecated | typical position: legacy stacks only
synonyms: zone system, ansel adams, zones, deprecated, legacy, tonal zones, dodge burn
purpose: A deprecated module that divided the tonal range into Ansel Adams-style zones
  and let each zone's boundaries be dragged.
use_when: only for legacy history stacks; tone equalizer is the modern zone-based tool.
