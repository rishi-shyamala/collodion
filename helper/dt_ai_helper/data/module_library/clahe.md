# local contrast (deprecated) (clahe)
group: tone | deprecated | typical position: legacy stacks only
synonyms: clahe, adaptive histogram equalization, local contrast old, deprecated, legacy, clarity
purpose: The deprecated contrast-limited adaptive histogram equalisation implementation
  of local contrast.
use_when: only for legacy history stacks; use local contrast (bilat) or contrast &
  texture instead.
